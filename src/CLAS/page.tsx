import React from 'react';

import ExampleComponent from 'CRM/pages/홈/Example';

// Example의 첫 진입점 역할을 해주는 페이지 입니다. 여기서 전체 레이아웃을 구성하고, 화면을 import해서 사용해주세요.
const Page = () => {
    return <ExampleComponent />;
};

export default Page;
